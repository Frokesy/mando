# Local browser requests remain same-origin at /api for reliable iPhone cookies.
# Next.js proxies them to the API running on this computer.
NEXT_PUBLIC_API_BASE_URL=/api
API_PROXY_TARGET=http://127.0.0.1:4000

# Development refuses remote API targets unless this is deliberately enabled.
ALLOW_REMOTE_API_IN_DEV=false
